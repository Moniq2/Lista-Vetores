/* Ler um vetor com 10 nomes de pessoas, após pedir
que o usuário digite um nome qualquer de pessoa. Escrever a mensagem “ACHEI”,
se o nome estiver armazenado no vetor C ou “NÃO ACHEI” caso contrário.
*/
#include<stdio.h>
int main(){
char vetor[10][20];
char nome[20];
for (int i = 0; i < 10; i++)
{
scanf("%s ", &vetor[i][i]);
}
scanf("%d", &nome[0]);
for (int i = 0; i < 10; i++)
{
if (vetor[i][i] == nome[0];)
{

}

}
 
return 0;
}